ktor {
    deployment {
        port = 8080
    }
    application {
        modules = [ com.example.ApplicationKt.module ]
    }
}

storage {
    driverClassName = "org.h2.Driver"
    jdbcURL = "jdbc:h2:file:./build/customerdb;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;"
}